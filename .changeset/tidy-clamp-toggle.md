---
'@hayasedb/ui': patch
'@hayasedb/web': patch
---

Show the description "Show more" toggle only when the clamped text actually overflows, measured from the rendered element instead of a character-count guess
