---
'@hayasedb/auth': minor
'@hayasedb/api': minor
'@hayasedb/nuxt': minor
---

Shared the auth session across the web and admin apps by deriving the cookie domain from the configured frontend URLs, and login pages now redirect already signed-in users to their destination
